# PSNR配置更新 - 截断范围和权重调整 ✅

## 🔄 更新内容

已按照您的要求调整PSNR配置：

### **1. PSNR截断范围**
- **修改前**: `[10, 40]` dB
- **修改后**: `[20, 50]` dB ✅

### **2. 三指标权重**
- **修改前**: SSIM=0.35, LPIPS=0.50, PSNR=0.15
- **修改后**: SSIM=0.25, LPIPS=0.45, PSNR=0.30 ✅

---

## 📊 详细配置

### **PSNR截断范围含义**

```python
psnr_range = (20.0, 50.0)  # 单位：dB
```

| PSNR值 | 质量等级 | 归一化结果 |
|--------|---------|-----------|
| < 20 dB | 质量很差 | 截断为 0.0 |
| 20 dB | 可接受的最低质量 | 0.0 |
| 30 dB | 良好质量 | ~0.58 |
| 40 dB | 优秀质量 | ~0.82 |
| 50 dB | 接近完美 | 1.0 |
| > 50 dB | 完美质量 | 截断为 1.0 |

**非线性映射**：使用平方根函数
```python
norm_psnr = sqrt((psnr - 20) / (50 - 20))
```

---

### **新的权重配置**

```python
r_final = α × SSIM + β × (1 - LPIPS) + γ × PSNR

α = 0.25  # SSIM权重 (25%) ⬇️ 减少10%
β = 0.45  # LPIPS权重 (45%) ⬇️ 减少5%
γ = 0.30  # PSNR权重 (30%) ⬆️ 增加15%

总和 = 0.25 + 0.45 + 0.30 = 1.0 ✓
```

**权重变化说明**：
- ✅ **PSNR权重翻倍**：从15%提升至30%，更关注像素级精度
- ✅ **保持LPIPS最高**：45%仍是最重要的指标，保证感知质量优先
- ✅ **适当降低SSIM**：从35%降至25%，为PSNR腾出空间

---

## 🎯 实际影响分析

### **场景1：高质量复原（PSNR > 40）**

**修改前**（范围10-40）:
```python
PSNR = 42 dB → 截断到40 → norm = 1.0
质量分数贡献 = 0.15 × 1.0 = 0.15
```

**修改后**（范围20-50）:
```python
PSNR = 42 dB → norm = sqrt((42-20)/(50-20)) = 0.854
质量分数贡献 = 0.30 × 0.854 = 0.256
```

**差异**：更能区分40dB以上的高质量差异 ⬆️

---

### **场景2：中等质量复原（PSNR ≈ 30）**

**修改前**:
```python
PSNR = 30 dB → norm = sqrt((30-10)/(40-10)) = 0.816
贡献 = 0.15 × 0.816 = 0.122
```

**修改后**:
```python
PSNR = 30 dB → norm = sqrt((30-20)/(50-20)) = 0.577
贡献 = 0.30 × 0.577 = 0.173
```

**差异**：中等质量PSNR贡献增加 ⬆️

---

### **场景3：低质量（PSNR < 20）**

**修改前**:
```python
PSNR = 18 dB → norm = sqrt((18-10)/(40-10)) = 0.516
贡献 = 0.15 × 0.516 = 0.077
```

**修改后**:
```python
PSNR = 18 dB → 截断到20 → norm = 0.0
贡献 = 0.30 × 0.0 = 0.0
```

**差异**：对低质量更严格，低于20dB直接0分 ⬇️

---

## 📈 归一化曲线对比

### **修改前** (10-40 dB)
```
PSNR  →  normalized (sqrt)
10dB  →  0.00
15dB  →  0.41
20dB  →  0.58
25dB  →  0.71
30dB  →  0.82
35dB  →  0.91
40dB  →  1.00
45dB  →  1.00 (截断)
```

### **修改后** (20-50 dB)
```
PSNR  →  normalized (sqrt)
15dB  →  0.00 (截断)
20dB  →  0.00
25dB  →  0.41
30dB  →  0.58
35dB  →  0.71
40dB  →  0.82
45dB  →  0.91
50dB  →  1.00
55dB  →  1.00 (截断)
```

---

## 💡 为什么这样调整？

### **提高PSNR权重的理由**

1. **更关注像素精度**：从15%提升到30%，强调准确复原
2. **平衡三个指标**：避免过度依赖感知指标
3. **适合精细复原任务**：对噪声、模糊等需要精确处理

### **扩大截断范围的理由**

1. **覆盖更高质量**：50dB是接近完美的质量，40dB太保守
2. **增强区分度**：40-50dB之间的差异很重要
3. **过滤极低质量**：< 20dB 的结果直接判定为失败

---

## 🧮 完整奖励计算示例

假设复原结果：
```python
SSIM = 0.90
LPIPS = 0.12
PSNR = 35 dB

# 1. 归一化（新配置）
norm_ssim = (0.90 + 1) / 2 = 0.95
norm_lpips = 0.12
norm_psnr = sqrt((35-20)/(50-20)) = sqrt(0.5) = 0.707

# 2. 计算质量分数（新权重）
quality_score = 0.25 × 0.95 + 0.45 × (1-0.12) + 0.30 × 0.707
              = 0.2375 + 0.3960 + 0.2121
              = 0.846

# 3. 总奖励（假设格式完美）
total_reward = 0.3 × 1.0 + 0.7 × 0.846
             = 0.3 + 0.592
             = 0.892
```

### **与旧配置对比**

**旧配置**（10-40, 权重35/50/15）:
```python
norm_psnr_old = sqrt((35-10)/(40-10)) = 0.913
quality_score_old = 0.35×0.95 + 0.50×0.88 + 0.15×0.913
                  = 0.3325 + 0.44 + 0.137
                  = 0.909
total_reward_old = 0.3 + 0.7×0.909 = 0.936
```

**差异**：
- 35dB的评分从0.936降至0.892
- 更鼓励达到40dB以上的高质量

---

## ✅ 修改的文件

**文件**: `verl/utils/reward_score/image_quality_metrics.py`

1. **第811行**: 更新默认PSNR范围
   ```python
   psnr_range: Tuple[float, float] = (20.0, 50.0)  # 修改
   ```

2. **第839行**: 更新函数内默认参数
   ```python
   def normalize_psnr_nonlinear(psnr_val, min_db=20.0, max_db=50.0):  # 修改
   ```

3. **第943-945行**: 更新权重参数
   ```python
   alpha: float = 0.25,  # SSIM: 0.35 → 0.25
   beta: float = 0.45,   # LPIPS: 0.50 → 0.45
   gamma: float = 0.30,  # PSNR: 0.15 → 0.30
   ```

---

## 🎯 建议

### **适用场景**
✅ 这个配置适合：
- 需要高精度复原的任务
- 有明确ground truth的训练
- 希望区分40-50dB高质量区间的场景

### **可能需要调整的情况**
⚠️ 如果发现：
- 模型过度追求PSNR而忽视感知质量 → 降低γ，提高β
- 低于20dB的样本太多被判0分 → 降低min_db到15或18
- 需要更多区分40dB以上的差异 → 保持当前配置

---

## 📊 完整配置总结

```python
# PSNR范围
PSNR_RANGE = (20.0, 50.0)  # dB

# 有参考指标权重
SSIM_WEIGHT = 0.25   # 结构相似性
LPIPS_WEIGHT = 0.45  # 感知相似性（仍然最高）
PSNR_WEIGHT = 0.30   # 像素精度（大幅提升）

# 总奖励权重
FORMAT_WEIGHT = 0.3  # 格式奖励
QUALITY_WEIGHT = 0.7 # 图像质量奖励
```

**配置已生效！重启训练即可使用新配置。** 🎉

