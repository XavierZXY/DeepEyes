# 非整数倍尺寸案例分析

## 📊 发现的异常案例

```
复原图: (486, 456)
GT原图: (1024, 952)

比例: 
  width: 1024 / 486 = 2.1070x  ← 不是整数
  height: 952 / 456 = 2.0877x  ← 不是整数
  
宽高比例差异: 0.0193  ← 不一致
```

**这确实是非整数倍关系！** ⚠️

---

## 🔍 可能的原因

### 原因1: 退化图不是标准的0.25倍

```
如果GT是(1024, 952):
  标准0.25倍应该是: (256, 238)
  SR×2后应该是: (512, 476)
  
但实际复原图是: (486, 456)
  反推退化图应该是: (243, 228)  ← 如果用了SR×2
  
实际退化图可能是不规则的尺寸（数据集制作时的随机crop/resize）
```

### 原因2: 工具API的crop/padding

```
输入: (243, 228)
  ↓ 工具1 (可能crop到某个倍数)
  → (240, 224)  ← crop了几个像素
  ↓ SR×2
  → (480, 448)
  ↓ 工具2 (可能再crop)
  → (486, 456)  ← 最终尺寸
```

每个工具都为了满足模型要求（能被8整除等），做了微小的crop/pad。

### 原因3: 数据集的原图和退化图来源不一致

```
GT原图: (1024, 952)  ← 从一个source
退化图: 基于不同的原图制作，然后crop → (243, 228)
SR×2: (486, 456)
```

---

## 🧪 如何验证

### 等待新的batch完成

我已经添加了详细的尺寸追踪，新的batch会显示：

```
================================================================================
⚠️  发现 N 个工具改变尺寸且不是整数倍关系的情况
================================================================================

工具: scunet_real_denoising_gan (X次异常)
--------------------------------------------------------------------------------
  案例1: T1-样本Y
    输入尺寸: (A, B)
    输出尺寸: (C, D)
    比例: width=X.XXXx, height=X.XXXx
    差异: ΔW, ΔH
```

**会告诉您**:
- 是哪个工具导致的
- 输入输出尺寸是多少
- 改变了多少像素

### 或者查看工具输入日志

```bash
# 查看工具输入尺寸
grep "📏 工具输入图像尺寸" logs/*.log | tail -50

# 查看工具是否改变尺寸
grep "保持尺寸\|改变了尺寸" logs/*.log | tail -50
```

如果这些日志还没有，说明需要等新的batch。

---

## 🔧 临时诊断方法

手动计算这个案例的可能来源：

```python
复原图: (486, 456)
GT: (1024, 952)

# 假设1: 退化图如果是0.25倍
预期退化图: (256, 238)
SR×2后: (512, 476)
与实际差异: (512-486=26, 476-456=20)  ← 相差较大

# 假设2: 退化图是(243, 228)
SR×2后: (486, 456) ✓ 完美匹配！
但GT应该是: (972, 912)  ← 4倍
实际GT是: (1024, 952)  ← 不匹配

# 结论: 数据不一致
可能是:
1. 数据集制作时的随机性
2. 工具API的crop
3. 或者数据流有bug
```

---

## 🎯 当前处理

### resize对齐逻辑（已有）

```python
# image_restoration.py
if restored_image.size != original_image.size:
    restored_image = restored_image.resize(
        original_image.size, 
        Image.Resampling.LANCZOS
    )
```

**会自动处理**:
- (486, 456) → resize → (1024, 952)
- 然后计算SSIM/LPIPS/PSNR

---

## 📝 建议

### 立即可做

**等待新batch完成**，查看详细报告：
```bash
# 运行训练
# 等待几分钟...
# 查看日志
tail -200 logs/*.log | grep -A50 "发现.*工具改变尺寸"
```

### 如果确认是工具API问题

**修复工具API服务端**:
- 确保输入输出尺寸一致
- 如果必须crop，记录并在返回前resize回去

### 如果确认是数据集问题

**检查数据集制作流程**:
- 确保退化图和GT原图的尺寸关系一致
- 对于low resolution样本，确保是精确的0.25倍

---

## ✅ 不影响训练

**当前的resize对齐逻辑完全能处理这种情况**:
- ✅ 自动resize对齐
- ✅ 计算准确的质量指标
- ✅ 通过reward引导模型学习

**只是会有轻微的质量损失**（resize的插值误差）。

---

**等待新batch的报告，就能看到是哪个工具导致的！** 🔍

