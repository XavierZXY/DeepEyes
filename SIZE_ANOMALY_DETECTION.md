# 尺寸异常检测 - 自动诊断

## 🎯 功能说明

**问题**: 工具处理后尺寸变化，但不是整数倍关系（不是2x, 3x, 4x）

**解决**: 自动收集异常，在batch结束时统一显示

---

## 🔍 检测逻辑

### 判断标准

```python
# 工具改变了尺寸
if input_size != output_size:
    
    # 计算缩放比例
    w_ratio = output_width / input_width
    h_ratio = output_height / input_height
    
    # 检查是否是整数倍
    is_integer_scale = (
        abs(w_ratio - round(w_ratio)) < 0.01 and  # 宽度接近整数
        abs(h_ratio - round(h_ratio)) < 0.01 and  # 高度接近整数
        abs(w_ratio - h_ratio) < 0.01             # 宽高比例一致
    )
    
    if not is_integer_scale:
        # 🔥 记录异常！
        record_anomaly(...)
```

### 异常案例

```
正常（SR工具）:
  输入: (217, 253)
  输出: (434, 506)  # 2倍
  比例: 2.000x, 2.000x
  ✅ 整数倍，不记录

异常:
  输入: (472, 420)
  输出: (465, 415)
  比例: 0.985x, 0.988x
  ⚠️  不是整数倍，记录！
```

---

## 📊 输出格式

### 有异常时

```
================================================================================
⚠️  发现 15 个工具改变尺寸且不是整数倍关系的情况
================================================================================

工具: scunet_real_denoising_gan (8次异常)
--------------------------------------------------------------------------------
  案例1: T1-样本12
    输入尺寸: (1188, 1800)
    输出尺寸: (1176, 1792)
    比例: width=0.990x, height=0.996x
    差异: Δwidth=-12, Δheight=-8
  
  案例2: T2-样本25
    输入尺寸: (980, 1456)
    输出尺寸: (976, 1456)
    比例: width=0.996x, height=1.000x
    差异: Δwidth=-4, Δheight=0
  
  案例3: T1-样本45
    输入尺寸: (1024, 768)
    输出尺寸: (1024, 760)
    比例: width=1.000x, height=0.990x
    差异: Δwidth=0, Δheight=-8
  
  ... 还有 5 个类似案例

工具: restormer_deraining (5次异常)
--------------------------------------------------------------------------------
  案例1: T1-样本8
    输入尺寸: (800, 600)
    输出尺寸: (792, 600)
    比例: width=0.990x, height=1.000x
    差异: Δwidth=-8, Δheight=0
  
  ... 还有 4 个类似案例

工具: retinexformer_sdsd_indoor (2次异常)
--------------------------------------------------------------------------------
  案例1: T3-样本67
    输入尺寸: (640, 480)
    输出尺寸: (640, 472)
    比例: width=1.000x, height=0.983x
    差异: Δwidth=0, Δheight=-8

================================================================================
⚠️  这些工具可能需要检查API服务端的实现
================================================================================
```

### 无异常时

```
✅ 所有工具都保持尺寸或按整数倍缩放（SR工具）
```

---

## 🔍 诊断建议

### 看到异常后的操作

1. **确认模式**:
   ```
   差异: -8, -12, -16 等（8的倍数）
   → API服务端可能在crop到8/16的倍数
   ```

2. **检查工具API服务**:
   ```bash
   # 检查相应工具的API实现
   # 例如：TOOL_SERVICE_IP:5008 (SCUNet)
   curl -X POST http://$TOOL_SERVICE_IP:5008/info
   ```

3. **临时方案**:
   - 当前的resize对齐逻辑会自动处理 ✅
   - 不影响训练，只是有微小的质量损失

4. **永久修复**:
   - 修改API服务端，确保输入输出尺寸一致
   - 或者在API返回前resize回原始尺寸

---

## 💡 可能的原因

### 1. 深度学习模型的对齐要求

很多模型要求输入能被特定数整除：

```python
常见要求:
- 能被8整除  ← 最常见
- 能被16整除
- 能被32整除
```

**处理方式**:
- Crop: 裁剪多余像素（1188 → 1176 = crop 12px）
- Pad: 填充到倍数（但会增大尺寸）

### 2. API服务端的实现

```python
# 某些API可能这样实现
def process_image(img):
    h, w = img.shape[:2]
    
    # Crop to multiple of 8
    new_h = (h // 8) * 8
    new_w = (w // 8) * 8
    
    img_cropped = img[:new_h, :new_w]
    
    # 模型处理
    result = model(img_cropped)
    
    return result  # 返回的是cropped尺寸
```

**结果**: 
- 输入 (1188, 1800) → crop到 (1184, 1800) → 模型 → 输出 (1184, 1800)
- 或者crop到 (1176, 1792) 如果要求更严格

---

## 📈 使用方法

### 运行训练

```bash
bash examples/agent/IRv2.sh
```

### 查看结果

**每个batch结束时会自动显示**:

```
如果有异常:
  ⚠️  发现 N 个工具改变尺寸且不是整数倍关系的情况
  + 详细的工具名称、案例、尺寸变化

如果没有异常:
  ✅ 所有工具都保持尺寸或按整数倍缩放
```

**不会在执行过程中打印**，避免刷屏！

---

## 🎯 预期发现

基于您的日志 `(472, 420) vs (804, 1020)`：

### 可能情况1: SR工具scale=2

```
退化图: (402, 510)  # 原始low resolution
  ↓ SR scale=2
  → (804, 1020)  ✅ 完美匹配GT
  ↓ 其他工具（去噪等）
  → crop到8的倍数 → (472, 420)?  ❌ 不对，这反而变小了
```

### 可能情况2: 工具API的crop

```
退化图: (201, 255)  # 原始（0.25倍）
GT原图: (804, 1020)  # 4倍
  ↓ 工具链开始
输入: (201, 255)
  ↓ 工具1 crop到8的倍数
  → (200, 248)
  ↓ 工具2 进一步crop
  → (472, 420)?
```

### 可能情况3: 数据混乱

```
image_history保存的不是预期的图像
```

---

## 🚀 下一步

运行训练，查看输出：

```bash
bash examples/agent/IRv2.sh 2>&1 | tee training.log

# 训练中不会有刷屏
# 每个batch结束时会看到统计信息
```

**查看**:
1. 哪些工具导致了异常
2. 异常的具体模式（差多少像素）
3. 是否有规律（8的倍数、16的倍数等）

---

**功能已添加！训练时会自动诊断并在batch结束时显示！** 🎉

