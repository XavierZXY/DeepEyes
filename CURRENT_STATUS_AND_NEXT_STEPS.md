# 当前状态和下一步 - 完整总结

## 📊 已验证的事实

### 1. 数据集：100%完美 ✅

```
检查了768个样本（3个shard）:
  ✅ 无low resolution: 退化图=GT（558个）
  ✅ 有low resolution: 退化图=GT/4（210个）
  ❌ 不精确关系: 0个
```

**数据集没有任何问题！**

### 2. 工具API：100%保持尺寸 ✅

```
测试了4种尺寸，2个工具:
  输入(924, 956) → 输出(924, 956) ✅
  输入(924, 952) → 输出(924, 952) ✅
  输入(800, 804) → 输出(800, 804) ✅
  输入(1024, 1020) → 输出(1024, 1020) ✅
```

**工具API完全不改变尺寸！**

### 3. 配置

```
batch_size: 32
n: 4  ← 每个样本生成4个响应
actual_size: 32（从日志）
```

---

## 🔍 仍然存在的问题

### 观察到的异常

**同一个GT对应多个明显不同的复原图**:

```
GT=(936, 812)对应:
  复原图1: (440, 466) → 比例 2.13x, 1.74x ← 宽高不一致！
  复原图2: (424, 390) → 比例 2.21x, 2.08x
  复原图3: (438, 402) → 比例 2.14x, 2.02x
```

**分析**:

#### 如果是正常的n=4 sampling:

```
同一个样本X:
  退化图: (234, 203)
  GT: (936, 812)
  
  生成4个响应:
    响应1: [去雨, SR×2] → (468, 406)
    响应2: [去噪, SR×2] → (468, 406)
    响应3: [去噪] → (234, 203)
    响应4: [SR×4] → (936, 812)
  
  特点: 复原图应该接近几个固定尺寸
    - (234, 203) - 没用SR
    - (468, 406) - SR×2
    - (936, 812) - SR×4
```

#### 但实际看到的:

```
复原图: (440, 466), (424, 390), (438, 402)
  
  这些尺寸彼此差异很大（不是来自相同的退化图）
  而且与GT=(936, 812)的比例都是2.0x左右但不精确
  
  → 说明这些复原图来自不同的原始退化图！
  → 但被分配了同一个GT！
  → GT索引还是有问题！
```

---

## 🔧 可能的原因

### 怀疑1: image_history和extra_info的interleave不一致

```python
image_history长度 = 32
extra_info长度 = 32
needs_interleave = 32 * 4 == 32? → False

→ 使用 orig_idx = i（直接对应）

但如果实际应该是:
  batch_size = 8
  n = 4  
  actual_size = 8 * 4 = 32
  
那么:
  image_history[0-3] 应该对应 样本0的4个响应
  image_history[4-7] 应该对应 样本1的4个响应
  
  extra_info[0-3] 也应该对应 样本0（同一个）
  extra_info[4-7] 应该对应 样本1（同一个）
```

### 怀疑2: 保存逻辑有问题

可能image_history保存的索引和extra_info不对应。

---

## 🎯 需要的调试信息

**下一个batch完成时会显示**:

```bash
[DEBUG GT SIZE] Sample 0: GT原图尺寸 = (???, ???)
[DEBUG GT SIZE] Sample 0: image_history长度 = ?
[DEBUG GT SIZE] Sample 0: 退化图尺寸(初始) = (???, ???)
[DEBUG GT SIZE] Sample 0: 复原图尺寸(最后) = (???, ???)

[DEBUG GT SIZE] Sample 1: GT原图尺寸 = (???, ???)
[DEBUG GT SIZE] Sample 1: image_history长度 = ?
[DEBUG GT SIZE] Sample 1: 退化图尺寸(初始) = (???, ???)
[DEBUG GT SIZE] Sample 1: 复原图尺寸(最后) = (???, ???)

[DEBUG GT SIZE] Sample 2: GT原图尺寸 = (???, ???)
[DEBUG GT SIZE] Sample 2: image_history长度 = ?
[DEBUG GT SIZE] Sample 2: 退化图尺寸(初始) = (???, ???)
[DEBUG GT SIZE] Sample 2: 复原图尺寸(最后) = (???, ???)
```

### 如何判断

**如果n=4且batch_size=8，应该看到**:

```
Sample 0, 1, 2, 3: GT都相同（来自同一个原始样本）
  例如: GT都是(936, 812)
  退化图(初始)都相同
  但复原图(最后)不同（4个不同的响应）

Sample 4, 5, 6, 7: GT都相同（来自另一个样本）
  例如: GT都是(868, 1012)
```

**如果看到Sample 0, 1, 2的GT各不相同**:
  → 说明batch_size更大，或者还有其他问题

---

## 📝 当前总结

```
已确认:
  ✅ 数据集：完美
  ✅ 工具API：完全不改变尺寸
  ✅ 索引修复：代码已修改
  
待确认:
  ⏳ GT索引是否真的修复了
  ⏳ 4像素差异从哪来
  ⏳ 为什么还有宽高比例不一致的情况
  
需要:
  等待下一个batch的GT SIZE详细日志
```

---

**等待新batch完成，查看GT SIZE日志！** 🔍

