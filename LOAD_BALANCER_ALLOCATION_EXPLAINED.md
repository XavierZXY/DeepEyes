# 负载均衡器分配机制详解

## 📌 核心机制

当前的负载均衡器采用**请求级别**的负载均衡，即**每次工具调用都独立分配服务器**，不考虑工具类型。

### 关键实现

```python
# 在每个工具类中（例如 SwinIRToolbox.py）
@property
def api_url(self):
    """动态获取API URL，支持负载均衡"""
    ip = get_tool_service_ip()  # 每次访问都会调用
    return f"http://{ip}:5001/process"
```

**重点**：
- `api_url` 是一个 `@property`，不是静态变量
- 每次访问 `self.api_url` 都会调用 `get_tool_service_ip()` 重新获取IP
- 工具调用API时（`requests.post(self.api_url, ...)`）会动态选择服务器

## 🎯 分配场景分析

### 场景1：连续两张图片调用相同工具

假设配置：
```bash
export TOOL_SERVICE_IPS="10.21.9.6,10.21.9.7"
export TOOL_LOAD_BALANCE_STRATEGY=round_robin
```

**执行流程**：

```
图片1 调用 SwinIR:
  → get_tool_service_ip() 返回 10.21.9.6
  → 请求发送到 http://10.21.9.6:5001/process
  → 服务器1的GPU卡0处理（假设SwinIR部署在卡0）

图片2 调用 SwinIR:
  → get_tool_service_ip() 返回 10.21.9.7
  → 请求发送到 http://10.21.9.7:5001/process
  → 服务器2的GPU卡0处理（假设SwinIR部署在卡0）

图片3 调用 SwinIR:
  → get_tool_service_ip() 返回 10.21.9.6
  → 请求发送到 http://10.21.9.6:5001/process
  → 再次使用服务器1的GPU卡0
```

**结果**：
- ✅ 两台服务器的 SwinIR 服务（端口5001）轮流处理
- ✅ 即使是同一个工具，也会均匀分配到不同服务器
- ✅ 避免某一台服务器的SwinIR-GPU过载

### 场景2：连续两张图片调用不同工具

**执行流程**：

```
图片1 调用 SwinIR (端口5001):
  → get_tool_service_ip() 返回 10.21.9.6
  → 请求发送到 http://10.21.9.6:5001/process
  → 服务器1的GPU卡0处理（SwinIR在卡0）

图片2 调用 NAFNet (端口5012):
  → get_tool_service_ip() 返回 10.21.9.7
  → 请求发送到 http://10.21.9.7:5012/process
  → 服务器2的GPU卡1处理（NAFNet在卡1）

图片3 调用 HAT (端口5010):
  → get_tool_service_ip() 返回 10.21.9.6
  → 请求发送到 http://10.21.9.6:5010/process
  → 服务器1的GPU卡2处理（HAT在卡2）
```

**结果**：
- ✅ 不同工具的请求也遵循轮询规则
- ✅ 充分利用所有服务器的所有GPU卡
- ✅ 即使调用不同工具，负载仍然均衡分配

### 场景3：复杂混合场景

假设一个batch有4张图片，需要执行以下工具调用：

```
图片A: SwinIR → NAFNet → HAT
图片B: SwinIR → Restormer
图片C: NAFNet
图片D: SwinIR → SwinIR
```

**轮询模式分配（2台服务器）**：

```
请求序列：
1. 图片A-SwinIR    → 10.21.9.6:5001 (服务器1-卡0)
2. 图片A-NAFNet    → 10.21.9.7:5012 (服务器2-卡1)
3. 图片A-HAT       → 10.21.9.6:5010 (服务器1-卡2)
4. 图片B-SwinIR    → 10.21.9.7:5001 (服务器2-卡0)
5. 图片B-Restormer → 10.21.9.6:5006 (服务器1-卡3)
6. 图片C-NAFNet    → 10.21.9.7:5012 (服务器2-卡1)
7. 图片D-SwinIR    → 10.21.9.6:5001 (服务器1-卡0)
8. 图片D-SwinIR    → 10.21.9.7:5001 (服务器2-卡0)
```

**统计**：
- 服务器1：4个请求（请求1, 3, 5, 7）
- 服务器2：4个请求（请求2, 4, 6, 8）
- **完美均衡！**

## 🔄 两种负载均衡策略对比

### 1. 轮询模式（round_robin）- 默认

**特点**：
- 严格按顺序分配：IP1 → IP2 → IP3 → IP1 → ...
- 保证绝对均衡
- 与工具类型无关

**示例**（3台服务器）：
```
请求1: SwinIR    → IP1
请求2: SwinIR    → IP2  (即使工具相同，也分配到不同服务器)
请求3: NAFNet    → IP3
请求4: SwinIR    → IP1
请求5: HAT       → IP2
请求6: SwinIR    → IP3
```

**优点**：
- ✅ 负载绝对均衡
- ✅ 可预测性强
- ✅ 适合长时间运行的训练任务

**缺点**：
- ⚠️ 如果某台服务器性能差，无法动态调整

### 2. 随机模式（random）

**特点**：
- 每次随机选择服务器
- 长期来看负载基本均衡
- 与工具类型无关

**示例**（3台服务器）：
```
请求1: SwinIR    → IP2 (随机)
请求2: SwinIR    → IP1 (随机)
请求3: NAFNet    → IP2 (随机)
请求4: SwinIR    → IP3 (随机)
请求5: HAT       → IP2 (随机)
请求6: SwinIR    → IP1 (随机)
```

**优点**：
- ✅ 简单高效
- ✅ 无需维护状态
- ✅ 天然支持服务器动态增减

**缺点**：
- ⚠️ 短期内可能不均衡
- ⚠️ 不可预测

## 💡 为什么这样设计？

### 设计原则1：工具类型无关性

**原因**：
- 不同样本可能调用不同工具组合
- 某个工具可能在某个时间段被频繁调用
- 如果按工具类型分配，可能导致某台服务器某个GPU过载

**好处**：
- 无论调用什么工具，负载都均衡
- 简单且通用，无需复杂的工具感知逻辑

### 设计原则2：请求级别负载均衡

**原因**：
- 每个工具调用是独立的
- 处理时间差异很大（取决于图像大小、复杂度）
- 请求级别负载均衡最细粒度，最灵活

**好处**：
- 即使连续调用同一工具，也不会压垮单台服务器
- 最大化并行处理能力

### 设计原则3：动态IP获取

**原因**：
- 使用 `@property` 而不是静态变量
- 每次调用时重新获取IP

**好处**：
- 支持运行时修改配置
- 天然支持多线程并发
- 无需复杂的状态管理

## 📊 性能分析

### 服务器GPU利用率

假设每台服务器有4个GPU，分别部署：
- GPU0: SwinIR (端口5001)
- GPU1: NAFNet (端口5012)
- GPU2: HAT (端口5010)
- GPU3: Restormer (端口5006)

**场景：100个请求，都调用SwinIR**

**单服务器模式**：
```
服务器1-GPU0: 100个请求（严重过载）
服务器1-GPU1/2/3: 空闲
```

**双服务器轮询模式**：
```
服务器1-GPU0: 50个请求
服务器2-GPU0: 50个请求
其他GPU: 空闲
```
- ✅ 吞吐量提升约2倍
- ✅ 单个GPU压力减半

**场景：100个请求，平均分布到4种工具**

**双服务器轮询模式**：
```
服务器1-GPU0: 约13个请求 (SwinIR)
服务器1-GPU1: 约12个请求 (NAFNet)
服务器1-GPU2: 约13个请求 (HAT)
服务器1-GPU3: 约12个请求 (Restormer)

服务器2-GPU0: 约12个请求 (SwinIR)
服务器2-GPU1: 约13个请求 (NAFNet)
服务器2-GPU2: 约12个请求 (HAT)
服务器2-GPU3: 约13个请求 (Restormer)
```
- ✅ 所有GPU均衡利用
- ✅ 整体吞吐量提升约2倍

## 🔧 特殊情况处理

### 情况1：某台服务器某个工具未部署

**问题**：
- 服务器1部署了所有工具
- 服务器2只部署了SwinIR、NAFNet

**后果**：
- 调用HAT时，如果分配到服务器2，会失败

**解决方案**：
```bash
# 方案1: 确保所有服务器部署相同工具（推荐）
# 方案2: 只配置完整部署的服务器
export TOOL_SERVICE_IPS="10.21.9.6"  # 只用完整的服务器
```

### 情况2：服务器性能差异

**问题**：
- 服务器1有4张A100 GPU
- 服务器2只有2张V100 GPU

**后果**：
- 轮询模式下，性能弱的服务器可能成为瓶颈

**解决方案**：
```bash
# 方案1: 使用随机模式（长期会自然均衡）
export TOOL_LOAD_BALANCE_STRATEGY=random

# 方案2: 只配置性能相近的服务器
export TOOL_SERVICE_IPS="10.21.9.6,10.21.9.8"  # 跳过性能差的服务器2
```

### 情况3：网络延迟差异

**问题**：
- 服务器1在本地（延迟1ms）
- 服务器2在远程（延迟50ms）

**后果**：
- 分配到远程服务器时，整体速度变慢

**解决方案**：
```bash
# 优先使用本地服务器
export TOOL_SERVICE_IPS="10.21.9.6"  # 只用本地服务器

# 或增加本地服务器的权重（未来功能）
# export TOOL_SERVICE_IPS="10.21.9.6,10.21.9.6,10.21.9.7"  # IP1权重2倍
```

## 🎓 最佳实践

### 1. 生产环境推荐配置

```bash
# 使用多台性能相同的服务器
export TOOL_SERVICE_IPS="10.21.9.6,10.21.9.7,10.21.9.8"

# 使用轮询策略确保均衡
export TOOL_LOAD_BALANCE_STRATEGY=round_robin

# 确保所有服务器：
# - 部署了完全相同的工具
# - 使用相同的端口配置
# - 网络延迟相近
# - GPU性能相近
```

### 2. 混合场景配置

如果有1台高性能服务器 + 2台普通服务器：

```bash
# 方案1: 都用（简单）
export TOOL_SERVICE_IPS="10.21.9.6,10.21.9.7,10.21.9.8"

# 方案2: 重复高性能服务器IP（增加权重）
export TOOL_SERVICE_IPS="10.21.9.6,10.21.9.6,10.21.9.7,10.21.9.8"
# 这样服务器1会处理约50%的请求，其他两台各25%
```

### 3. 调试配置

```bash
# 开发测试时使用单服务器
export TOOL_SERVICE_IP=localhost

# 或只用一台服务器简化问题排查
export TOOL_SERVICE_IPS="10.21.9.6"
```

## 📈 监控建议

### 关键指标

1. **每台服务器的请求数**
   - 查看负载是否均衡
   - 轮询模式应该完全均衡

2. **每个GPU的利用率**
   - 查看是否有GPU闲置
   - 查看是否有GPU过载

3. **平均响应时间**
   - 对比单服务器和多服务器模式
   - 验证性能提升

4. **失败率**
   - 某台服务器故障会导致部分请求失败
   - 需要及时移除故障服务器

### 监控脚本示例

```python
# 在训练脚本中添加
from tool_load_balancer import get_all_tool_service_ips
from collections import Counter

# 初始化计数器
request_counter = Counter()

# 每次工具调用后记录
def log_request(tool_name, ip):
    request_counter[f"{ip}:{tool_name}"] += 1

# 定期输出统计
def print_stats():
    print("\n=== 负载均衡统计 ===")
    for key, count in request_counter.most_common():
        print(f"{key}: {count} 次")
```

## 🔮 未来优化方向

### 1. 工具感知的负载均衡

```python
# 按工具类型分别维护轮询索引
# 例如：SwinIR在服务器1, NAFNet在服务器2
# 优点：更精细的控制
# 缺点：复杂度增加
```

### 2. 性能感知的负载均衡

```python
# 根据服务器响应时间动态调整分配
# 快的服务器分配更多请求
# 优点：自动适应性能差异
# 缺点：需要额外的监控和状态管理
```

### 3. 会话保持（Session Affinity）

```python
# 同一张图片的多个工具调用使用同一台服务器
# 优点：减少图像传输
# 缺点：可能导致负载不均
```

### 4. 加权轮询

```python
# 支持配置服务器权重
export TOOL_SERVICE_IPS_WEIGHTED="10.21.9.6:2,10.21.9.7:1"
# 服务器1处理2/3的请求，服务器2处理1/3
```

## 📚 相关文档

- `TOOL_LOAD_BALANCER_GUIDE.md`: 负载均衡器使用指南
- `LOAD_BALANCER_QUICK_START.md`: 快速开始指南
- `verl/workers/agent/envs/mm_process_engine/tool_load_balancer.py`: 实现代码

---

**总结**：当前的负载均衡器采用简单高效的**请求级别轮询/随机分配**策略，不区分工具类型，每次工具调用都独立选择服务器。这种设计简单、通用、有效，能够在大多数场景下实现良好的负载均衡效果。

**最后更新**: 2025-10-31  
**作者**: DeepEyes Team

